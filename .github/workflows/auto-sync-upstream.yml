name: Auto Sync Upstream

on:
  schedule:
    # 每天 UTC 2:00 (北京时间 10:00) 自动同步上游
    - cron: '0 2 * * *'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史，以便正确合并
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/lbjlaq/Antigravity-Manager.git || true
          git fetch upstream

      - name: Check for updates
        id: check
        run: |
          # 检查上游是否有新提交
          UPSTREAM_COMMIT=$(git rev-parse upstream/main)
          CURRENT_COMMIT=$(git rev-parse HEAD)

          echo "upstream_commit=$UPSTREAM_COMMIT" >> $GITHUB_OUTPUT
          echo "current_commit=$CURRENT_COMMIT" >> $GITHUB_OUTPUT

          if [ "$UPSTREAM_COMMIT" != "$CURRENT_COMMIT" ]; then
            BEHIND_COUNT=$(git rev-list --count HEAD..upstream/main)
            echo "has_updates=true" >> $GITHUB_OUTPUT
            echo "behind_count=$BEHIND_COUNT" >> $GITHUB_OUTPUT
            echo "✅ 发现 $BEHIND_COUNT 个新提交需要同步"
          else
            echo "has_updates=false" >> $GITHUB_OUTPUT
            echo "✅ 已是最新版本，无需同步"
          fi

      - name: Backup custom files
        if: steps.check.outputs.has_updates == 'true'
        run: |
          mkdir -p /tmp/custom-backup

          # 备份 daily-build.yml
          if [ -f ".github/workflows/daily-build.yml" ]; then
            cp .github/workflows/daily-build.yml /tmp/custom-backup/
            echo "✅ 已备份 daily-build.yml"
          fi

          # 备份 tauri.conf.json 中的自定义 pubkey
          if [ -f "src-tauri/tauri.conf.json" ]; then
            PUBKEY=$(jq -r '.plugins.updater.pubkey // empty' src-tauri/tauri.conf.json)
            if [ -n "$PUBKEY" ]; then
              echo "$PUBKEY" > /tmp/custom-backup/tauri-pubkey.txt
              echo "✅ 已备份 tauri pubkey"
            fi
          fi

      - name: Sync with upstream
        if: steps.check.outputs.has_updates == 'true'
        run: |
          # 方式1: 尝试正常合并
          if git merge upstream/main --no-edit -m "chore: sync with upstream $(git rev-parse --short upstream/main)"; then
            echo "✅ 成功通过 merge 同步上游更新"
            echo "sync_method=merge" >> $GITHUB_ENV
          else
            echo "⚠️ Merge 失败，尝试 rebase..."
            git merge --abort || true

            # 方式2: 使用 rebase
            if git rebase upstream/main; then
              echo "✅ 成功通过 rebase 同步上游更新"
              echo "sync_method=rebase" >> $GITHUB_ENV
            else
              echo "❌ Rebase 也失败了，使用强制重置策略"
              git rebase --abort || true

              # 方式3: 强制重置到上游（保留自定义文件）
              git reset --hard upstream/main
              echo "✅ 已强制重置到上游最新版本"
              echo "sync_method=reset" >> $GITHUB_ENV
            fi
          fi

      - name: Restore custom files
        if: steps.check.outputs.has_updates == 'true'
        run: |
          # 恢复 daily-build.yml
          if [ -f "/tmp/custom-backup/daily-build.yml" ]; then
            mkdir -p .github/workflows
            cp /tmp/custom-backup/daily-build.yml .github/workflows/
            echo "✅ 已恢复 daily-build.yml"
            if ! git diff --quiet .github/workflows/daily-build.yml; then
              git add .github/workflows/daily-build.yml
              git commit -m "chore: restore custom daily-build workflow for Windows ARM64 support" || true
            fi
          fi

          # 恢复 tauri.conf.json 中的自定义 pubkey
          if [ -f "/tmp/custom-backup/tauri-pubkey.txt" ] && [ -f "src-tauri/tauri.conf.json" ]; then
            SAVED_PUBKEY=$(cat /tmp/custom-backup/tauri-pubkey.txt)
            jq --arg pubkey "$SAVED_PUBKEY" '.plugins.updater.pubkey = $pubkey' \
              src-tauri/tauri.conf.json > /tmp/tauri.conf.json.tmp
            mv /tmp/tauri.conf.json.tmp src-tauri/tauri.conf.json
            echo "✅ 已恢复 tauri pubkey"
            if ! git diff --quiet src-tauri/tauri.conf.json; then
              git add src-tauri/tauri.conf.json
              git commit -m "chore: restore custom tauri signing pubkey" || true
            fi
          fi

      - name: Push changes
        if: steps.check.outputs.has_updates == 'true'
        run: |
          if [ "${{ env.sync_method }}" = "reset" ] || [ "${{ env.sync_method }}" = "rebase" ]; then
            git push origin main --force
            echo "✅ 已强制推送更新到 origin/main"
          else
            git push origin main
            echo "✅ 已推送更新到 origin/main"
          fi

      - name: Summary
        if: always()
        run: |
          echo "## 🔄 同步报告" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.check.outputs.has_updates }}" = "true" ]; then
            echo "### ✅ 同步成功" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- **同步方法**: ${{ env.sync_method }}" >> $GITHUB_STEP_SUMMARY
            echo "- **上游提交**: \`${{ steps.check.outputs.upstream_commit }}\`" >> $GITHUB_STEP_SUMMARY
            echo "- **落后提交数**: ${{ steps.check.outputs.behind_count }}" >> $GITHUB_STEP_SUMMARY
            echo "- **自定义文件**: daily-build.yml, tauri pubkey (已保留)" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ℹ️ 无需同步" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "当前已是最新版本" >> $GITHUB_STEP_SUMMARY
          fi
